/**
 * Represents a source of gravity.
 */
export default class Gravity {

    /**
     * Creates a new source of gravity.
     * @todo there will have to be force and impulse modelling
     *
     * @param  {Array}  coordinates  Gravity source coordinates, [x, y]
     * @param  {Number}  force  Force strength
     */
    constructor (coordinates = [0, 0], force = 1) {

        
    }
}